{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [{ "src": "/(.*)", "dest": "/$1" }],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "projectSettings": {
    "framework": null,
    "devCommand": "npm run dev",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  }
}
